# ── Database (server only) ─────────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gbyke_erp
DB_USER=postgres
DB_PASSWORD=change_me

# ── JWT ────────────────────────────────────────────────────────
SECRET_KEY=generate_a_64_char_hex_string_here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480

# ── Server bind (server only) ──────────────────────────────────
SERVER_HOST=0.0.0.0
SERVER_PORT=8000

# ── Client: set this to the camera PC's LAN IP ────────────────
SERVER_IP=127.0.0.1

# ── First-run admin seed (server only, used once) ─────────────
ADMIN_USERNAME=your_admin_username
ADMIN_PASSWORD=your_strong_password
ADMIN_FULL_NAME=Full Name
